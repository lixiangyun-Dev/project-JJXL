server:
  port: 8088
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jjxl?useUnicode=true&characterEncoding=utf8&useSSL=false
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver

#Mybatis 开启控制台打印sql语句
mybatis:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    #开启驼峰命名
    map-underscore-to-camel-case: true

localUploadPathImg: E:\image\     #本地路径